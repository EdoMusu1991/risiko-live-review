{
  "name": "@risiko/eventi-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Schema condiviso degli eventi di partita Risiko (zod). Usato da Battle Commander (replay) e Risiko Live Review (frontend + export).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
